{
  "name": "coherence",
  "outputDir": "public",
  "entryDir": ".",
  "tooling": [
    "test"
  ],
  "ignore": [
    "node_modules",
    ".git",
    "dist",
    ".turbo",
    ".wrangler"
  ],
  "codeExt": [
    "ts"
  ],
  "typecheck": [
    "npm",
    "run",
    "typecheck"
  ],
  "test": [
    "node",
    "scripts/run-named-test.mjs"
  ],
  "language": "typescript",
  "platform": null,
  "sources": [
    "src",
    "test"
  ],
  "testDir": "test/",
  "testMatch": "pass [1-9]",
  "oracleExecution": "serial",
  "mass": {
    "measures": [
      {
        "key": "graph-components",
        "cmd": [
          "node",
          "scripts/graph-population.mjs",
          "components"
        ],
        "unit": "components"
      },
      {
        "key": "graph-claims",
        "cmd": [
          "node",
          "scripts/graph-population.mjs",
          "claims"
        ],
        "unit": "claims"
      }
    ]
  },
  "conventions": {
    "dismissed": {
      "isActive": "A hover/selection highlight predicate inside the browser script render-contract.ts embeds as a string. It decides which lane is lit, grades nothing, and cannot make a verdict unearned.",
      "isProse": "redundancy.ts's site-kind classifier — it only weights a ranked ADVISORY that gates nothing. A site mis-weighted here changes the order of a report, never a verdict.",
      "isTooling": "render-outline.ts's display grouping — which subtree prints under `tooling` in the outline. Presentation over an already-derived graph; the graph itself is unaffected.",
      "isPyTestPath": "One shared definition exported from oracle-domain.ts and imported where python test files must be excluded (novelty surface, redundancy walk). A single spelling, not a scattered convention — and it scopes advisories, never a verdict.",
      "scopeKeyOf": "redundancy.ts's chain-grouping key — which enclosing scope a compare chain belongs to, computed by capture containment. It shapes how an ADVISORY groups candidates; it grades nothing and cannot make a verdict unearned.",
      "canonicalEntry": "pi-control.ts's package-relative native extension entry computes the one controlled settings value used by inspection and installation; it is a single path convention at the host boundary, not an unvalidated external input."
    }
  },
  "sinks": {
    "safeHtml": "(^|[^.\\w])(escapeXml|escapeAttr|esc)\\(|\\.toFixed\\(|^[A-Z][A-Z0-9_]*$|^-?\\d"
  },
  "atlas": {
    "charts": {
      "project-source": "The repository under analysis as it is on disk: *.spec.md prose, source text, coherence.config.json, git history. Agent-authored and arbitrary — never trusted as a statement about itself, only as text to be read.",
      "derived-model": "The graph the harness builds from that text — components, claims, symbols, import edges — plus the config it was walked with. Everything downstream is computed over this and nothing re-reads the tree.",
      "shell": "The other side of a process boundary: the commands the harness spawns for evidence (test runner, typecheck, project `measures` probes) and the hook process the agent harness spawns to reach the journal. Nothing here is the harness's own memory, and an exit code is the only fact that crosses.",
      "record": "The durable memory a LATER run compares against — .coherence/status.json, the pinned baselines in public/, the decision journal. Every ratchet in this repo is a comparison against this chart, which is why a byte of garbage in it is a gate that went quiet.",
      "verdict": "What a gate is permitted to SAY: a claim's green/red/skip, a ratchet's held/failed, the process exit code. The scarcest chart in the system — a fact reaches it only through a crossing that earned it.",
      "reading-surface": "What a human or an agent then reads: public/_*.html, AGENTS.md, CLAUDE.md, the terminal reports. Derived text with no authority of its own; it can mislead, but it cannot turn a red green."
    },
    "transitions": {
      "buildGraph": {
        "from": "project-source",
        "to": "derived-model",
        "security": true,
        "translates": "The single ingress: arbitrary repository text becomes the model every downstream gate is computed over. Nothing after this re-reads the tree, so whatever the walk drops is not wrong here — it is absent everywhere, silently. floor.ts's non-vacuity refusal exists because of exactly this crossing, and grades the collapse rather than the crossing."
      },
      "cloudflareBindings": {
        "from": "project-source",
        "to": "derived-model",
        "security": true,
        "translates": "Direct module Env / Cloudflare.Env store types and wrangler configuration become one canonical platform domain. Source/config agreement deduplicates, disagreement refuses, deployment-only store metadata cannot perturb node bytes, runtime variables cross as names with a typed declared marker rather than values, and source inference receives the graph's filtered file population so generated local declarations cannot become a hidden second walk."
      },
      "loadConfig": {
        "from": "project-source",
        "to": "shell",
        "security": true,
        "translates": "The repository's own coherence.config.json becomes argv the harness spawns — `test`, `typecheck`, and every `mass.measures` command. Running the harness in a tree executes that tree's config; readJsonOrRefuse makes an unparseable config refuse rather than revert to defaults, but a parseable one is obeyed."
      },
      "readJsonOrRefuse": {
        "from": "record",
        "to": "derived-model",
        "security": true,
        "translates": "The one door every remembered fact enters through — status.json, each ratchet baseline, the journal. It separates ABSENT (legitimate: first run, nothing pinned) from UNREADABLE (never legitimate), because the `catch → return default` it replaced turned a single garbage byte into every floor on the page quietly agreeing."
      },
      "readDefects": {
        "from": "record",
        "to": "derived-model",
        "security": true,
        "translates": "Committed defect rows become admissible controller evidence only after their exact shape, self-consistent content address, session placement, Git object-name shape, and stable regular-file placement agree. Pre-existing symlinks, malformed or internally inconsistent surviving rows, and terminal-capable provenance refuse the whole corpus rather than entering a reading surface; Git history, not the in-row unkeyed hash, witnesses valid rewrites or deletion."
      },
      "readTrustedJournal": {
        "from": "record",
        "to": "derived-model",
        "security": true,
        "translates": "Surviving decision rows become verdict-bearing evidence only when repository containment, complete entry inventory, canonical append framing, version, exact shape, canonical time, content identity, terminal references, duplicate agreement, and portable session/file attribution all hold. At zero rows, current Git HEAD distinguishes deleted tracked evidence from genuine adoption. A human may still use the tolerant historical render, but damage makes the trusted projection unavailable rather than silently shrinking it."
      },
      "readWork": {
        "from": "record",
        "to": "derived-model",
        "security": true,
        "translates": "Per-session work events become one coordination graph only after their content identities, predecessor chain, ownership handoffs, dependency and parent graph, terminal evidence, and contained append paths agree. State is replayed rather than overwritten; competing or detached histories refuse."
      },
      "readConsequences": {
        "from": "record",
        "to": "derived-model",
        "security": true,
        "translates": "Assessor-authored relationship rows become navigation edges only after exact typed endpoints, relation admissibility, evidence, content identity, session placement, duplicate agreement, and contained regular-file placement agree. Timestamp, path, and commit proximity never enter this crossing as inferred edges."
      },
      "observeOrientation": {
        "from": "record",
        "to": "derived-model",
        "security": true,
        "translates": "Independent strict decision, work, consequence, experiment, defect, and runtime-validated verification readings become one deterministic swarm heading. Any unavailable source outranks an apparently empty action list; missing verification provenance stays stale, collisions and dangling addresses remain named, and synthesis is selected only when the representing parent close can execute. The projection executes nothing."
      },
      "analyzeDecisionPositions": {
        "from": "record",
        "to": "derived-model",
        "security": true,
        "translates": "Standing trusted decisions become swarm policy positions only through explicit subject and authority fields. Similar prose and timestamp order contribute nothing; local disagreement requires ratification and incompatible choices tied at the highest authority remain contested."
      },
      "validateWorkGraph": {
        "from": "record",
        "to": "derived-model",
        "security": true,
        "translates": "Replayed work records become an admissible coordination graph only when parent and dependency references are acyclic and ordered, terminal parents have no live children, and every completed direct child is explicitly synthesized. Prospective writes cross this same validation before append."
      },
      "detectWorkScopeOverlaps": {
        "from": "record",
        "to": "derived-model",
        "security": true,
        "translates": "Declared exact and subtree write addresses become potential overlaps or live collisions according to dependency readiness. The projection names both owners and scopes; it never upgrades an overlap into permission or infers undeclared semantic coupling."
      },
      "createWork": {
        "from": "shell",
        "to": "record",
        "security": true,
        "translates": "A caller's objective, authority, criteria, graph references, and repository scopes become an immutable work opening only after exact writer identity, canonical shape, contained scope, and single-line data checks hold. Invalid prospective graph state refuses before append."
      },
      "repeatedSingletonFlags": {
        "from": "shell",
        "to": "verdict",
        "security": true,
        "translates": "Repeated CLI arguments become either declared plural evidence and scope values or a refusal. Session, owner, authority, predecessor, and other singleton selectors never silently become last-wins attribution for a ledger append."
      },
      "traceConsequences": {
        "from": "record",
        "to": "reading-surface",
        "security": false,
        "translates": "Strict explicit consequence edges become bidirectional navigation around an optional focus while every row retains its assessed relation and authored direction. The trace adds no edge from timestamp, path, commit, or record co-presence."
      },
      "relationProblem": {
        "from": "record",
        "to": "verdict",
        "security": true,
        "translates": "An assessor's relation verb and endpoint kinds become an admission or refusal. Specialized lifecycle verbs accept only their declared source-target pairs; the generic relations remain available when no stronger causal grammar is warranted."
      },
      "verifyOrientation": {
        "from": "record",
        "to": "derived-model",
        "security": true,
        "translates": "Persisted verification JSON plus the live Git tree become never, stale, failing, or current only after canonical time, commit shape, dirty bit, tier, and nonnegative failure count validate. Current additionally requires matching commit addresses and no material live change; the status receipt itself is excluded so filing evidence does not self-invalidate."
      },
      "sourceTextIsNavigable": {
        "from": "project-source",
        "to": "verdict",
        "security": true,
        "translates": "Every live TypeScript source blob becomes admissible searchable input only when it contains no NUL byte. A source that common repository tools classify as binary fails the guard instead of silently disappearing from agent navigation."
      },
      "analyzeOracle": {
        "from": "project-source",
        "to": "verdict",
        "security": true,
        "translates": "An oracle's own source is read to decide whether it iterates a LIVE domain or a literal. A test that loops a hardcoded list proves nothing about the code and must not be allowed to green a totality claim."
      },
      "runNamedTest": {
        "from": "derived-model",
        "to": "shell",
        "security": true,
        "translates": "A claim's oracle NAME becomes a test selection in a spawned runner. A name that exists nowhere exits nonzero — the vanished oracle cannot pass by selecting nothing."
      },
      "execNamedTest": {
        "from": "shell",
        "to": "verdict",
        "security": true,
        "translates": "A subprocess's exit code and stdout become a claim's verdict. Exit 0 is not evidence: the output must positively match `testMatch`, so a runner that ran nothing fails instead of greening the renamed test."
      },
      "resolveFromBatch": {
        "from": "shell",
        "to": "verdict",
        "security": true,
        "translates": "One batched runner's output is split back into per-claim verdicts. ZERO matching tests is its own named state, never a quiet pass — the batch's efficiency must not buy back the absence it would otherwise hide."
      },
      "resolveStaticOracle": {
        "from": "derived-model",
        "to": "verdict",
        "security": true,
        "translates": "Current conventional Vitest source names plus concrete owners from modified or deleted test paths at Git HEAD become a tri-state existence verdict without executing test configuration or modules. A current literal match proves existence only; complete current absence or loss from a deleted/still-complete former owner is VANISHED ORACLE (static). A former owner that itself becomes dynamic or damaged remains UNKNOWN, as do recognized extended, runtime-registered, custom-included, unreadable, and otherwise incomplete sources. Projects assembled beyond that grade disable staticOracleExistence or use a fresh report."
      },
      "runVerify": {
        "from": "derived-model",
        "to": "verdict",
        "security": true,
        "translates": "The graded claims plus coverage become the run's verdict. A declared invariant with no anchoring boundary is a promise with no chokepoint, and it reds coverage rather than being counted as satisfied."
      },
      "vacuityRefusal": {
        "from": "derived-model",
        "to": "verdict",
        "security": true,
        "translates": "The floor beneath every other crossing on this map: before any claim is graded, the derived surface is checked against what the record remembers. An empty derivation over a remembered one REFUSES — `0 of 0` never renders as coherent."
      },
      "recordVerify": {
        "from": "verdict",
        "to": "record",
        "security": true,
        "translates": "This run's verdicts become the memory the next run compares against — the only writer to that chart. A skip merges rather than overwrites, so a scoped run can never launder a real red into an absence."
      },
      "setLifecycleHookForHost": {
        "from": "derived-model",
        "to": "project-source",
        "security": true,
        "translates": "One host-selected lifecycle contract becomes that host's tracked project settings, stable launcher, and root mapping. Claude and Codex share the five event meanings without sharing settings syntax, matchers, launcher bytes, or bundle identity; unrelated host configuration survives the convergence."
      },
      "resolveLanguageAdapter": {
        "from": "project-source",
        "to": "shell",
        "security": true,
        "translates": "The config's `language` value becomes the adapter whose grammar derives the entire graph — a built-in name, or a project module imported and EXECUTED from the tree under analysis (the same declared trust as loadConfig's argv). An unknown name refuses with the live built-in list rather than falling back to the typescript grammar, because a wrong adapter walks a different tree than the one configured and every downstream gate grades the garbage with full confidence."
      },
      "makeTreeSitterAdapter": {
        "from": "project-source",
        "to": "derived-model",
        "security": true,
        "translates": "A grammar wasm — typically shipped by a tree-sitter grammar package, addressed by the project's adapter module — is loaded and EXECUTED to parse the tree under analysis into graph symbols, import edges, and prose. The wasm is sandboxed to parsing but it is still a project-supplied executable artifact, the same declared trust as the config's argv and adapter modules; a load or query failure refuses at adapter resolution rather than deriving a partial graph."
      },
      "surfaceOfSource": {
        "from": "project-source",
        "to": "derived-model",
        "security": false,
        "translates": "One file's text becomes counted behavioral surface — exported symbols, domain variants, keyed-table keys — for both languages the harness reads. The change signal's zero-anchor alarm consumes these counts, so a language this crossing cannot see grows silently; python is read at the declared regex grade rather than not at all."
      },
      "analyzeParityOracle": {
        "from": "project-source",
        "to": "verdict",
        "security": true,
        "translates": "A parity oracle's own source decides whether it enumerates the LIVE shared domain or a hand-copied sample. TypeScript is walked by compiler; python by the same indent-block discipline as analyzePythonOracle — and in both, a literal list roots at nothing, a missing oracle is not-found, and neither can green a totality claim."
      },
      "sitesOfPython": {
        "from": "project-source",
        "to": "derived-model",
        "security": false,
        "translates": "Python enumeration sites — module dicts, Enum bodies, string-constant collections, match/elif chains — become candidate token sets for the undeclared-duplication ranker, the same shape the TS extractor emits. Advisory only; with no compiler behind .py, precision is earned at extraction because nothing downstream can rescue a false positive."
      },
      "lintSinks": {
        "from": "project-source",
        "to": "verdict",
        "security": true,
        "translates": "Raw interpolations into dangerous contexts — JS template literals and python f-strings alike — become the ratcheted review surface. A NEW unreviewed site fails --check; the safe-pattern config grades the bare expression identically in both languages, so switching languages cannot shrink the visible surface."
      },
      "reconcile": {
        "from": "record",
        "to": "verdict",
        "security": true,
        "translates": "The pinned interpolation-site review is re-addressed against today's tree. A moved file keeps its baselined identity; a COPIED site finds nothing to absorb it and is still novel, so review cannot be laundered by duplication."
      },
      "reconcileMass": {
        "from": "record",
        "to": "verdict",
        "security": true,
        "translates": "The pinned mass is re-addressed after renames. A vanished pin is inherited only when family, unit and EXACT value all match — a rename that also grew absorbs nothing, so growth cannot ride in under a relabel."
      },
      "auditPremiseLeases": {
        "from": "record",
        "to": "verdict",
        "security": true,
        "translates": "A standing decision's named structural addresses are re-resolved against today's graph. A decision whose premise no longer exists is reported expired rather than silently continuing to hold."
      },
      "signal": {
        "from": "project-source",
        "to": "verdict",
        "security": false,
        "translates": "A diff's shape becomes a demand: behavioral growth with zero anchors needs an invariant or an explicit attestation. It grades process, not evidence — a tear here delays a claim, it does not fabricate one."
      },
      "selectRegulation": {
        "from": "derived-model",
        "to": "verdict",
        "security": true,
        "translates": "Live sensor observations become exactly one ordered intervention. Missing, foreign, or conflicting readings refuse; a required decision outranks a mechanical redirect; and release means only that the versioned rules evaluated no obligation. A tear here can mask stronger debt with easier work, so the selector's priority is a guarded crossing rather than formatter policy."
      },
      "observeRegulation": {
        "from": "project-source",
        "to": "verdict",
        "security": true,
        "translates": "The selected agent host's lifecycle surface plus strict swarm orientation become the observations consumed by doctrine v2. A complete Claude path cannot satisfy a Codex reading (or vice versa); damaged coordination evidence fails closed; live work collisions and completed work without an explicit verification link become obligations rather than being inferred away."
      },
      "isActivityRow": {
        "from": "record",
        "to": "derived-model",
        "security": true,
        "translates": "An append-only activity row becomes admissible evidence only when its host, transport, session relation, event identity, canonical time, and optional command result agree. A torn or internally contradictory row is counted as damage instead of entering status, calibration, or experiment inference."
      },
      "closeExperiment": {
        "from": "shell",
        "to": "record",
        "security": false,
        "translates": "An assessor's action and criterion evidence becomes an immutable close row joined to the frozen owner plan. Every planned id must be answered exactly once, trace and activity prefixes must remain intact, each telemetry window names its weakest provable scope (none, owner-session, parent-session-aggregate, and legacy-unscoped for older trace), and the outcome is derived from criterion statuses; malformed, partial, rewritten, unreadable, or internally unscoped evidence refuses instead of becoming history."
      },
      "recordDefect": {
        "from": "shell",
        "to": "record",
        "security": true,
        "translates": "An agent's explicit defect assessment becomes one content-addressed session record carrying required evidence and repository provenance. Exact retries dedupe; malformed, internally inconsistent, or session-displaced surviving rows refuse the merged read, while committed Git history remains the external witness for valid rewrites or clean deletion. The crossing stores accountable testimony and labels it agent-assessed — it does not manufacture machine proof, cryptographic immutability, or causal attribution."
      },
      "tailJournal": {
        "from": "record",
        "to": "reading-surface",
        "security": false,
        "translates": "The journal's append-only session files become a live feed and a surfable history. Identity comes from a record's own content rather than its byte position, so a compaction fold neither drops nor duplicates what a watcher sees; the crossing carries memory out to a reader and never writes a verdict back."
      },
      "calibrate": {
        "from": "record",
        "to": "reading-surface",
        "security": false,
        "translates": "Recorded read-traces and labeled outcomes become a report on how well the context predictor did. It gates nothing — the crossing carries a measurement out to a reader, never back into a verdict."
      },
      "calibrationPaths": {
        "from": "record",
        "to": "derived-model",
        "security": false,
        "translates": "A host trace and the shared worktree become one calibration sample domain without upgrading scope: exact session writes, Codex parent-session aggregates, legacy unscoped rows, and shared-worktree fallback remain distinct."
      },
      "contextFor": {
        "from": "derived-model",
        "to": "reading-surface",
        "security": false,
        "translates": "The graph becomes the smallest packet an agent needs for one task. Bounded and byte-stable, and every approximation it makes is named in the packet — a reader must be able to see what was left out."
      },
      "renderContextProjection": {
        "from": "derived-model",
        "to": "reading-surface",
        "security": false,
        "translates": "A task-addressed context packet becomes a byte-bounded reading surface in stable route-first order. Mandatory framing and named limitations are never truncated; every withheld item and byte is accounted for, and a budget too small for that accounting refuses with its exact minimum."
      },
      "runHook": {
        "from": "shell",
        "to": "record",
        "security": false,
        "translates": "A lifecycle event the selected agent host hands this process becomes durable journal state or a transient read/activity row carrying host, transport, and bundle identity. It writes memory but never a verdict, so a tear loses history rather than earning a green."
      },
      "assignedWorkInstructions": {
        "from": "record",
        "to": "reading-surface",
        "security": true,
        "translates": "Repository-authored work data becomes a model startup instruction only for the exact owner session. Authority, criteria, write scope, blockers, and collisions are control-byte neutralized before crossing into that agent's context; another session's assignment does not. An unreadable ledger becomes a named unavailable message rather than a lifecycle failure."
      },
      "composeHookText": {
        "from": "project-source",
        "to": "reading-surface",
        "security": false,
        "translates": "Project-authored override and append files become part of the text injected into an agent's context at each lifecycle event: base = override ?? canonical, the append follows, and an unreadable file degrades to the canonical emission — a torn customization costs the project's voice, never the session. It can mislead an agent, but it cannot touch a verdict."
      },
      "currentObservation": {
        "from": "record",
        "to": "verdict",
        "security": true,
        "translates": "One explicitly named session's activity becomes the activation reading used by session-scoped hook checks. Only canonical host transport events from the selected host and current installed bundle count as observed; direct probes, stale bundles, and other sessions cannot earn activation. Parent-fallback may prove that exact bundle reached the parent session, but it cannot establish which child produced the event."
      },
      "registerPiHooks": {
        "from": "shell",
        "to": "record",
        "security": false,
        "translates": "Pi's native extension events become exact-session lifecycle activity and journal state without a launcher or pi-subagents dependency; main settlement remains silent while an optional child emits one guarded follow-up turn."
      },
      "inspectPiLifecycleHook": {
        "from": "project-source",
        "to": "verdict",
        "security": true,
        "translates": "The Pi project settings package entry, piProjectRoot mapping, and runnable extension target become one structural native control bit; unrelated or missing settings cannot masquerade as the host control."
      },
      "inspectLifecycleHook": {
        "from": "project-source",
        "to": "verdict",
        "security": true,
        "translates": "The selected host's project hook surface is read to produce ONE structural control bit: Claude's shared and local settings, or Codex's hooks.json plus the relevant project config.toml controls, together with that host's aligned launcher root, root mapping, and runnable target. Host-specific syntax cannot weaken singularity: a checker that accepts two firing paths, an inline competitor, a disabled feature, or managed-only exclusion is not a binary control, only a substring detector with a nicer report."
      },
      "requireDeclaredRoot": {
        "from": "project-source",
        "to": "verdict",
        "security": true,
        "translates": "The presence or absence of coherence.config.json becomes permission to walk. Absent refuses with the one-line bootstrap ({} declares the root) rather than walking whatever directory the shell was in and grading that population with full confidence — the measured incident was a home directory."
      }
    },
    "nonTransition": {
      "builtinLanguagePacks": "Aggregates the five in-harness pack tables for inspection and the purity oracle. No crossing: harness constants in, harness constants out — it exists so the pack-purity guard has one perimeter to sweep and so conformance checking has a future home.",
      "withTree": "Memory hygiene within the shell chart: owns a parse tree's whole wasm lifetime and frees it in a finally, so node captures cannot outlive their tree and a leak is unrepresentable. No fact crosses a chart here."
    }
  }
}
